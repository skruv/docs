## Step 4: Using components

Since skruv uses normal JS you use the normal way of importing components. As long as they return a vDOM object or a function, promise or generator that resolves/returns a vDOM object it works. In this step we have moved the input and text to it's own component called NameBox.
