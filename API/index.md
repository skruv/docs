# Skruv API

Skruvs API consists of three files, `vDOM.js`, `state.js` and `html.js`.

* vDOM.js handles rendering to DOM.
* state.js handles state and triggering updates when state changes.
* html.js helps build HTML & SVG vDOM objects to pass to vDOM.js.
